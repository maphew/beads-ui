# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm

# intermediate sync & merge files
*.base.*
*.left.*

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Keep JSONL exports and config (source of truth for git)
!*.jsonl
!metadata.json
!config.json
